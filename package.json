{
  "private": true,
  "name": "miniflare-168",
  "version": "1.0.0",
  "description": "A template for kick starting a Cloudflare Workers project",
  "main": "index.js",
  "scripts": {
    "miniflare": "miniflare index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "license": "MIT",
  "devDependencies": {
    "miniflare": "^2.13.0",
    "prettier": "^1.18.2"
  }
}
